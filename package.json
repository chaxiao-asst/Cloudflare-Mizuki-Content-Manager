{
  "name": "mizuki-content-manager-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for managing Mizuki blog content in GitHub",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "build": "wrangler build"
  },
  "dependencies": {
    "itty-router": "^4.0.23"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.4.5",
    "wrangler": "^4.90.0"
  }
}